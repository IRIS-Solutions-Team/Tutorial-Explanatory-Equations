
!equations(:elephant :blackbird)

    :rattlesnake
    x = @(1) + @(2)*x{-1} + @(3)*a{-2};

    :zebra
    difflog(y) = @(1) + @(2)*difflog(y{-1}) + sqrt(abs(@(2)+@(3)))*b{-1} + @(3)*c;

!equations(:snail)

    z = log( d^@(1) * e{-1}^(1-@(1)) );

    % Identity
    w === x + y + z;

